A simple SQL query example to retrieve distinct film data from a PostgreSQL database

## SQL Code

SELECT DISTINCT
    film_id,
    title,
    description,
    release_year,
    language_id,
    rental_duration,
    rental_rate,
    length,
    replacement_cost,
    rating,
    last_update,
    special_features,
    fulltext
FROM
    film;
